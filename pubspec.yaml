name: train_sns_app
description: "This is a practice app for learning Flutter"
publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1
environment:
  sdk: ^3.7.2
dependencies:
  flutter:
    sdk: flutter
  http: ^1.4.0
  flutter_riverpod: ^2.5.1
  go_router: ^14.1.0
  firebase_core: ^3.1.1
  firebase_auth: ^5.1.1
  freezed_annotation: ^2.4.1
  json_annotation: ^4.9.0
  cupertino_icons: ^1.0.8
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
flutter:
  uses-material-design: true
